.sc-abstract > .se-title {
  font-size: var(--h1-font-size);
  margin-bottom: var(--h1-font-size);
}

.sc-abstract .sc-abstract > .se-title {
  font-size: var(--h2-font-size);
  margin-bottom: var(--h2-font-size);
}

.sc-abstract .sc-abstract .sc-abstract > .se-title {
  font-size: var(--h3-font-size);
  margin-bottom: var(--h3-font-size);
}

.sc-abstract > .se-abstract {
  font-size: var(--h1-font-size);
  margin-bottom: var(--h1-font-size);
}

.sc-abstract .sc-abstract > .se-title {
  font-size: var(--h2-font-size);
  margin-bottom: var(--h2-font-size);
}

.sc-abstract .sc-abstract .sc-abstract > .se-title {
  font-size: var(--h3-font-size);
  margin-bottom: var(--h3-font-size);
}

package com.insilico.dmc.user

enum RoleEnum {

    ADMIN,
    INSILICO_CURATOR,
    MOD_CURATOR,
    PUBLISHER_PAID,
    PUBLISHER_UNPAID,
    TIER1_USER,
    TIER2_USER,
    TIER3_USER,
    PUBLIC,

    static constraints = {
    }
}
